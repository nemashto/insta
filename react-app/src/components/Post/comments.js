import React from "react";
import { AddComment } from "./addComment";

export const Comments = ({id, comments: allComments, createdAt, commentImput}) => {

    return (
        <>
            <div className="p-4 pt-1 pb-4">
                
            </div>
            <AddComment />
        </>
    )
}